module.exports = function expressMiddleware (router) {
  // setup local fixture server here
  //e.g.
  // router.use('/url/being/hit', (req, res) => {
  //   const response = require('../tests/fixtures/' + req.query.partNumber + '.json');
  //   res.json(response);
  // })
}
